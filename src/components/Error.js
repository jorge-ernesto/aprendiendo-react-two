import React from 'react'

export const Error = () => {
    return (
        <section id="content" className="error">
            <h2 className="subheader">Página no encontrada</h2>
            <p>La página a la que intentas acceder no existe en la web</p>
        </section>
    )
}
